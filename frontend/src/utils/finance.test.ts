import {describe,expect,it} from 'vitest';import {financialYearRange,pageRange,toggleValue} from './finance';
describe('frontend utilities',()=>{it('calculates the current financial year before April',()=>expect(financialYearRange(new Date('2026-02-15'))).toEqual({from:'2025-04-01',to:'2026-03-31'}));it('calculates the current financial year after April',()=>expect(financialYearRange(new Date('2026-07-13'))).toEqual({from:'2026-04-01',to:'2027-03-31'}));it('calculates pagination range',()=>expect(pageRange(2,10,24)).toEqual({from:11,to:20}));it('toggles a multi-select without duplicates',()=>{expect(toggleValue(['a'],'b')).toEqual(['a','b']);expect(toggleValue(['a','b'],'a')).toEqual(['b'])});});
