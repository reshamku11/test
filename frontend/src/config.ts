export const DEFAULT_PAGE_SIZE = 6;
export const PAGE_SIZE_OPTIONS = [6, 10, 20, 50, 100] as const;
export const TOAST_DURATION_MS = 3_000;
export const CALENDAR_DAYS = 10;
export const CALENDAR_HOURS = Array.from({ length: 10 }, (_, index) => index + 8);
